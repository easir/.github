name: Docker Image CI

on:
  workflow_call:
    secrets:
      npmrc2:
        required: false

jobs:
  build:
    runs-on: self-hosted
    steps:
    - uses: actions/checkout@v3
    - name: fetch-npmrc
      run: echo ${{ secrets.npmrc2 }} > ./.npmrc
    - name: Build the Docker image
      run: |
        TAG=$(date +%s)
        docker build . --file Dockerfile --tag my-image-name:$TAG --network=host
